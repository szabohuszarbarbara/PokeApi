import React from 'react';

function Pagination(props) {
    return (
        <div>
            <button>Previous</button>
            <button>Next</button>

        </div>
    );
}

export default Pagination;